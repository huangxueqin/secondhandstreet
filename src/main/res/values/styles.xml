<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="popupmenu">
        <item name="android:windowEnterAnimation">@anim/menu_show</item>        <!-- 指定显示的动画xml  -->
        <item name="android:windowExitAnimation">@anim/menu_hide</item>       <!-- 指定消失的动画xml  -->
    </style>

</resources>
